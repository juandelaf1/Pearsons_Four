from pathlib import Pathimport pandas as pdimport streamlit as stBASE = Path(__file__).resolve().parent.parentDATA_DIR = BASE / 'data'@st.cache_datadef load_global():    path = DATA_DIR / 'global' / 'global_salaries_unified.csv'    if path.exists():        df = pd.read_csv(path)        df['salary_usd'] = pd.to_numeric(df['salary_usd'], errors='coerce')        return df    return pd.DataFrame()@st.cache_datadef load_spain():    path = DATA_DIR / 'espana' / 'spain_salaries_2026_unified.csv'    if path.exists():        df = pd.read_csv(path)        df['salary_eur'] = pd.to_numeric(df['salary_eur'], errors='coerce')        return df    return pd.DataFrame()@st.cache_datadef load_linkedin():    path = DATA_DIR / 'linkedin_data_normalized.csv'    if path.exists():        df = pd.read_csv(path)        df['normalized_salary'] = pd.to_numeric(df['normalized_salary'], errors='coerce')        return df    return pd.DataFrame()@st.cache_datadef load_ppp():    path = DATA_DIR / 'eurostat' / 'ppp_rates.csv'    if path.exists():        return pd.read_csv(path)    return pd.DataFrame()@st.cache_datadef load_analytics(name):    path = DATA_DIR / 'analytics' / name    if path.exists():        return pd.read_csv(path)    return pd.DataFrame()def get_global_filters(df):    roles = sorted(df['role_normalized'].dropna().unique())    seniorities = sorted(df['seniority'].dropna().unique())    countries = sorted(df['country_name'].dropna().unique())    return roles, seniorities, countriesdef filter_global_df(df, roles, seniorities, countries, salary_range):    mask = pd.Series(True, index=df.index)    if roles:        mask &= df['role_normalized'].isin(roles)    if seniorities:        mask &= df['seniority'].isin(seniorities)    if countries:        mask &= df['country_name'].isin(countries)    if salary_range:        mask &= df['salary_usd'].between(salary_range[0], salary_range[1])    return df[mask].copy()